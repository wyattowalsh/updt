# Plugin System

Understanding updt's plugin architecture.

## Available Plugins

updt includes 16 built-in plugins for different package managers.

See {doc}`../cli` for the complete list.

## Plugin Architecture

All plugins inherit from `PluginBase` and implement a standard interface.

See {doc}`../api/index` for API documentation.
